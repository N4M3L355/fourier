{
  "name": "fourier",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "develop": "webpack --mode development --watch",
    "build": "webpack --mode production",
    "browserify": "browserify -r tonal-detect > bundle.js"
  },
  "dependencies": {
    "tonal": "latest",
    "tonal-detect": "latest"
  }
}
